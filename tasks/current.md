# Current

Consolidação de modelagem concluída (`docs/analise-regras-negocio.md`, `decisions/ADR-006` a `ADR-009`, `tasks/backlog.md`).

Próximo: TASK-001 (`operador` + `cargo` + `Empresa.tipo`) — aguardando decisão pendente de escopo do operador (`todas` vs `atribuídas`).
