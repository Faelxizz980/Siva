// Ainda não existe tabela `alerta` em db/db.sql — a regra de detecção de
// vazamentos/consumo anômalo é item das "Próximas etapas" do projeto.
// Este arquivo existe para já fixar onde essa persistência vai entrar.
export {};
