#pragma once

// Copie este arquivo para secrets.h e preencha com os valores reais antes de
// gravar no ESP32. secrets.h é gitignored — nunca commitar credenciais reais.

#define WIFI_SSID "SUA_REDE"
#define WIFI_PASSWORD "SUA_SENHA"

// IP da máquina que roda o backend, na mesma rede Wi-Fi do ESP32.
// Não usar "localhost" nem endereço de nuvem/codespace — o ESP32 não alcança.
#define API_HOST "192.168.0.10"
